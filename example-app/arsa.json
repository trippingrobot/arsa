{
  "name": "example-app",
  "handler": "users.handler",
  "authorization": {
    "handler": "users.authorize",
    "type": "lambda",
    "token_source": "method.request.header.x-arsa-token",
    "token_validation": ".*",
    "ttl": 300
  }
}
